# Python
2026 파이썬 입문
01. 파이썬 스타터 팩 (Intro)컴퓨터가 돌아가는 기본 메커니즘을 이해하고, 기계와 소통하기 위해 파이썬이라는 도구를 쥐었습니다.왜 파이썬인가: 문법이 직관적이라 직관적으로 코드를 짜기 좋고, 강력한 라이브러리 덕분에 확장성이 무궁무진하다는 걸 체감했습니다.플레이그라운드: 주피터 노트북과 구글 코랩을 활용해 인터랙티브하게 코드를 실행하며 삽질을 시작했습니다.💡 기억할 점: 파이썬은 대소문자를 아주 까칠하게 구분합니다. print()를 Print()로 쓰는 순간 에러를 마주하게 됩니다. 확장자는 무조건 .py!Python# 내 인생 첫 파이썬 한 줄
print("Hello Python")
02. 변수와 연산: 데이터 다루기의 시작변수는 단순히 데이터를 담는 박스가 아니라, 메모리 속 객체의 주소를 가리키는 이름표(참조)라는 점이 신선했습니다. 파이썬은 선언 없이 대입만 해도 타입을 알아서 지정해 줘서 편하네요.자주 쓴 산술 연산자: +, -, *, / 외에 몫을 구하는 //, 나머지를 구하는 %, 제곱을 하는 까지 손에 익혔습니다.형 변환(Type Casting): input()으로 들어오는 데이터는 무조건 문자열이라, 계산할 땐 반드시 int()나 float()로 감싸 가공해야 한다는 필터링 로직을 배웠습니다.Pythonx, y = 100, 200
result = x + y
print(result) # 300

# 유저 입력값 받아서 정수로 바꾸기
user_num = int(input("숫자를 입력하세요: "))
03. 조건문: 분기점 만들기조건문이 없다면 프로그램은 일차선 도로 같았을 겁니다. 특정 상황에 맞춰 흐름을 제어하는 법을 익혔습니다. 파이썬은 특이하게 중괄호가 아닌 들여쓰기(Indentation)로 구역을 나누기 때문에 탭(Tab)과 스페이스바에 예민해져야 합니다.핵심 구문: if - elif - else조건의 조합: and(모두 참), or(하나라도 참), not(반대) 논리 연산자를 엮어 복잡한 전제 조건을 만드는 연습을 했습니다.Pythonscore = 80
if score >= 60:
    print("합격")
else:
    print("불합격")
04. 반복문: 컴퓨터에게 노가다 시키기컴퓨터가 인간보다 압도적으로 잘하는 '단순 반복'을 제어하는 파트입니다. for문과 while문을 다루며 알고리즘다운 코드를 짜기 시작했습니다.for문 & range(): 횟수 기반 반복에 최적화된 조합입니다.range(5) $\rightarrow$ [0, 1, 2, 3, 4]range(1, 6) $\rightarrow$ [1, 2, 3, 4, 5]range(1, 10, 2) $\rightarrow$ [1, 3, 5, 7, 9] (2칸씩 점프)while문: 조건이 만족하는 동안 무한 동력을 제공하지만, 탈출 조건(count += 1)을 빼먹으면 무한 루프 지옥에 빠지니 조심해야 합니다.Python# 지정 횟수 반복
for i in range(5):
    print("방문을 환영합니다!")

# 조건 만족시까지 반복
count = 0
while count < 5:
    print(count)
    count += 1
05. 함수: 모듈화의 첫걸음매번 복잡하게 구현하던 코드 조각을 하나의 기능(def)으로 묶어 재사용하는 법을 배웠습니다. 유지보수가 편한 깔끔한 코드를 짜기 위한 필수 관문이었습니다.구조: 값을 넘겨주는 매개변수(Parameter)와 연산 결과를 가로채서 밖으로 던져주는 반환값(return)의 흐름을 파악했습니다.Pythondef add(x, y):
    return x + y

print(add(10, 20)) # 30
06. 자료구조 I: 리스트 (List)연관된 데이터를 묶어서 한 번에 관리하는 가장 대중적인 시퀀스 타입입니다. 0번부터 시작하는 인덱싱과 마이너스(-) 부호로 뒤에서부터 접근하는 방식이 유용했습니다.리스트 조작 스킬 셋:추가/삽입: .append(), .insert()삭제: .remove(), .pop()유틸리티: .sort(), len(), max(), min(), sum()슬라이싱([:]): 리스트의 특정 범위를 유연하게 썰어내는 기술을 연마했습니다.Pythonarr = [0, 1, 2, 3, 4]
print(arr[1:3]) # [1, 2] 추출
print(arr[2:])  # [2, 3, 4] 끝까지 추출
07. 자료구조 II: 컬렉션 삼총사 & 문자열데이터의 목적(중복 허용 여부, 수정 가능 여부 등)에 따라 골라 써야 하는 심화 자료구조들을 비교해 보았습니다.튜플 (Tuple): 수정 불가능(Immutable). 데이터 오염을 막을 때 씁니다. (원소가 하나일 땐 (1,) 처럼 콤마를 찍어 야 하는 규칙 체크)세트 (Set): 중복 제거 필터로 최고입니다. 순서가 없고 집합 연산이 가능합니다.딕셔너리 (Dictionary): Key-Value 쌍 구조로, 사전형 데이터를 매핑해 초고속으로 조회할 때 씁니다.문자열: 문자열도 결국 글자들의 리스트(시퀀스)라 인덱싱과 슬라이싱이 그대로 먹힌다는 걸 확인했습니다.Python# 딕셔너리로 연락처 매핑하기
phone_book = {"Kim": "010-1234-5678", "Lee": "010-1111-2222"}
print(phone_book["Kim"]) # 키값으로 바로 매칭
08. 객체와 클래스 (OOP 개념)드디어 맛본 객체지향 프로그래밍! 추상적인 개념을 현실의 물건처럼 모델링하는 설계 과정을 실습했습니다.클래스: 속성과 행동을 정의하는 붕어빵 틀(설계도).__init__() (생성자): 인스턴스가 생성되는 타이밍에 속성값을 강제로 주입하고 세팅해 주는 특수 메서드입니다.self: 헷갈리기 쉽지만, '지금 만들어져서 움직이는 바로 그 객체 자신'을 뜻하는 마법의 키워드입니다.Pythonclass Car:
    def __init__(self, model, color):
        self.model = model
        self.color = color
        self.speed = 0 # 초기화

    def speed_up(self):
        self.speed += 10

# 이 구조를 응용해 자동차, 사람, TV 등 다양한 도메인을 코드로 구현하며 종강했습니다!
💡 상세 실습 소스코드와 매주 제출한 과제물은 상단 폴더에서 주차별/날짜별로 확인하실 수 있습니다.
