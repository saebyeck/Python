중간점검 1
1. 답: 함수는 특정 작업을 수행하는 명령어들의 모음에 이름을 붙인 것이다.
2. 답: 프로그램을 작성하다 보면 같은 처리를 반복해야 하는 경우가 많이 생기는데 이미 작성한 코드를 재활용하여 사용할 수 있으면 편하기 때문이다.

중간점검 2
1. 답: 인수
2. 답: 매개변수
3. 답: def test():
	num1 = int(input("첫 번째 정수를 입력하시오:"))
	num2 = int(input("두 번째 정수를 입력하시오:"))
	return num1, num2

중간점검 3
1. 답: def get_rect_area(w, h):
	area = w * h
	return area
2. 답: def main():
	result = get_rect_area(w, h)

중간점검 4
1. 답: 인수는 함수를 호출할 때 전달하는 값, 매개변수는 함수를 정의할 때 사용하는 변수다.
2. 답: 디폴트 인수는 함수의 매개변수의 기본값이다. 예) def(name, msg="안녕하세요")
3. 답: 키워드 인수는 인수의 이름을 명시적으로 지정해서 값을 매개 변수로 전달하는 방법이다. 예) def sub(x,y,z):, sub( 15, 25, 30 )
4. 답: 가변 인수라는 의미다.

중간점검 5
1. 답: return
2. 답: def addsub(x,y):
	 sum1 = x+y
	 sum2 = x-y
	 return sum1, sum2

중간점검 #6
1. 답: 지역변수는 함수 내부에서만 사용할 수 있지만, 전역변수는 함수 외부에서도 사용할 수 있다.
2. 답: 예 (가능하다)
3. 답: global 키워드를 쓰지 않으면 함수 안에서만 전역 변수의 값이 바뀌고 함수 밖에서는 바뀌지 않는다.